# 11 — Security, Governance, and Permissions

Repository writes are least-privilege and path/scope constrained. Secrets are never copied into patches/logs. Destructive migrations, broad dependency upgrades, production changes, and permission changes require explicit packets/authority.

DEVOS cannot mark its own patch production-proven.