# 12 — Testing, SECA, and Quality

DEVOS runs developer checks: unit/integration tests, lint, typecheck, build, targeted regressions, migration checks as relevant. Results are evidence but release acceptance belongs to SECA/release gates.

Tests must target the defect and guard against recurrence.