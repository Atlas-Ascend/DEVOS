# 07 — Packet OS Integration

DEVOS consumes build/repair packets and returns structured repair results. Scope widening, newly discovered defects, or architectural changes become linked child packets rather than hidden extra work.

A repair does not close its packet until required verification/proof returns.